pipeline {
    agent any
    stages {
        stage('Build') {
            steps {

                sh 'docker build -t showmethereturn:latest .'

              dir('kubernetes')  {
                sh 'kubectl create -f showmethereturn.yaml'
              }

            }
        }

    }
}
