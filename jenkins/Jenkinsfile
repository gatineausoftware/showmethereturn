pipeline {
    agent any
    stages {
        stage('Build') {
            steps {

                sh 'docker build -t showmethereturn .'

              dir('kubernetes')  {
                sh 'kubectl create -f showmethereturn.yaml'
              }

            }
        }

    }
}
